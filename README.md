# Lição 06 — Todo List React avançado

Aplicação com `TodoProvider` e Context API, `useState`, `useEffect` para persistência em localStorage, hook customizado `useLocalStorage`, filtros com `useMemo` e item memoizado com `React.memo`.

## Executar

```bash
npm install
npm run dev
```
